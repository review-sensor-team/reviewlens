# Architecture (initial notes)

- Backend: FastAPI serving JSON API.
- Frontend: Vue 3 + Vite single-page app.
- Communication: REST endpoints; later add auth, DB, and tests.

Next steps
- Add database (SQLite / Postgres) and ORM (SQLModel / SQLAlchemy).
- Add CORS, env config, and Dockerfiles.
